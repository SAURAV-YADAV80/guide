import { FC } from "react";

interface AcademicsProps {

}

const Academics:FC<AcademicsProps>=({}) => {
    return(
        <div>
        this is academics page.
        </div>
    )
}

export default Academics;