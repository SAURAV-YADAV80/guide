import { FC } from "react";

interface DailyTodoProps {

}

const DailyTodo:FC<DailyTodoProps>=({}) => {
    return(
        <div>
        this is DailyTodo page.
        </div>
    )
}

export default DailyTodo;